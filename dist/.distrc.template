#!/bin/bash

: ${OSCOPTS:="-A https://api.opensuse.org"}
: ${PROJECT:=openSUSE:Tools:Devel}
: ${PACKAGE:=obs-server-svn}
: ${TARPACKAGE:=obs-server}
: ${TARGET:=openSUSE_11.0}
: ${ARCH:=i586}
: ${SPECFILE:=}
# files from $PWD that are not part of the distribution, one on a line
: ${EXCLUDES:=README.SETUP README.UPDATERS TODO .dist* obs-server-test.spec.template obs.conf.template sysconfig.obs-worker.template }
: ${SVNDIR:=../../buildservice}
: ${OBSREV:=1.1.0}

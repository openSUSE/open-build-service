workflow:
  steps:
    - branch_package:
        source_project: OBS:Server:Unstable
        source_package: obs-server
        target_project: home:bs-team
  filters:
    event: pull_request
    architectures:
      only:
        - x86_64
    repositories:
      only:
        - openSUSE_Factory
        - 15.3

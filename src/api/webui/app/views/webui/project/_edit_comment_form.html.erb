<%= form_tag({:action => :edit_comment, :project => @project, :package => @package }, :id => 'edit_comment_form', remote: true) do %>
  <%= text_field_tag('text', @comment) %>
  <%= hidden_field_tag('last_comment', @comment) %>
  <%= submit_tag 'Save' %>
<% end %>


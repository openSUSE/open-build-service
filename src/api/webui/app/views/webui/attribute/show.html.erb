<% @pagetitle = "Show attributes" %>
<% project_bread_crumb
  @crumb_list << link_to(@package, :controller => 'package', :action => :show, :project => @project.name, :package => @package) if @package
  @crumb_list << 'Attributes' %>

<% if @package %>
  <%= render :partial => "package/tabs" %>
<% else %>
  <%= render :partial => "project/tabs" %>
<% end %>

<%= render :partial => "attributes" %>
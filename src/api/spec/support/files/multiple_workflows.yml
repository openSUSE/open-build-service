workflow123:
  steps:
    - branch_package:
        source_project: "test-project"
        source_package: "test-package"
        target_project: 'test-target-project'

workflow124:
  steps:
    - branch_package:
        source_project: "test-project"
        source_package: "test-package"
        target_project: 'test-target-project'

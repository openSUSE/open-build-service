%p
  = event['commenter'].realname
  wrote in request #{event['number']}:
%div
  = render_as_markdown(event['comment_body'])
%p
  = link_to('View Comment', request_show_url(event['number'], anchor: "comment-#{event['id']}", only_path: false, host: @host))

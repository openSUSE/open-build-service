<% bystring = ''
   if @e['by_user']
     bystring = "by user #{@e['by_user']}"
   elsif @e['by_group']
     bystring = "by group #{@e['by_group']}"
   elsif @e['by_package']
     bystring = "by package maintainers of #{@e['by_project']}/#{@e['by_package']}"
   else
     bystring = "by project maintainers of #{@e['by_project']}"
   end -%>
Request <%= @e['id'] %> (by <%= @e['author'] %>) requires a review <%= bystring %>
  Visit <%= url_for(controller: 'webui/request', action: :show, id: @e['id'], host: @host, only_path: false) %>

<% if @e['comment'] -%>
Review reason set by <%= @e['who'] %>:
<%= @e['comment'] %>

<% end -%>
Request description:
<%= @e['description'] %>

<%= render partial: 'actions' -%>

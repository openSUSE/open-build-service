%p
  = event['commenter'].realname
  wrote in report ##{event['report_id']}:
%div
  = render_as_markdown(event['comment_body'])
%p
  = link_to('View Comment', report_url(event['report_id'], anchor: "comment-#{event['id']}", only_path: false, host: @host))

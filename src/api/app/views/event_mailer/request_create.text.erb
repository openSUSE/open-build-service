Visit <%= url_for(controller: 'webui/request', action: :show, number: @e['number'], host: @host, only_path: false) %>

<% if @e['description']-%>
Description:
<%= @e['description']%>

<%-end-%>
<%= render partial: 'actions' %>

To REVIEW against the previous version:
   osc request show --diff <%= @e['number']%>

To ACCEPT the request:
   osc request accept <%= @e['number']%> --message="reviewed ok."
   
To DECLINE the request:
   osc request decline <%= @e['number']%> --message="declined for reason xyz (see ... for background / policy / ...)."

To REVOKE the request:
   osc request revoke <%= @e['number']%> --message="retracted because ..., sorry / thx / see better version ..."

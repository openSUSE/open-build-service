:ruby
  url = link_to("#{event['project']}/#{event['package']}", package_show_url(event['project'], event['package'], only_path: false, host: @host))
#{event['who']} unassigned #{event_relationship_recipient(event)} from #{url}.
= render partial: 'event_mailer/footer', locals: { event: event, receiver_role: receiver_role, event_type: event_type }

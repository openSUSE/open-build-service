<%= event['commenter'].realname %> wrote in project <%= event['project'] %>:

<%= event['comment_body'] %>

View Comment at <%= project_show_url(event['project'], anchor: "comment-#{event['id']}", only_path: false, host: @host) %>
<%= render partial: 'event_mailer/footer', locals: { event: event, receiver_role: receiver_role, event_type: event_type } %>

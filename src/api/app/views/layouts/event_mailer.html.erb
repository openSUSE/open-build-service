<%= yield -%>
<br/>
-- <br/>
Configure notifications at <%= link_to(*[url_for(controller: 'webui/user', action: :notifications, only_path: false, host: @host)]*2) %><br/>
<%= @configuration['title'] %> (<%= link_to(*[url_for(controller: 'webui/main', action: :index, only_path: false, host: @host)]*2) %>)<br/>

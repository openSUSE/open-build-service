<%= yield -%>

-- 
Configure notifications at <%= url_for(controller: 'webui/user', action: :notifications, only_path: false, host: @host) %>
<%= @configuration['title'] %> (<%= url_for(controller: 'webui/main', action: :index, only_path: false, host: @host) %>)

- if CONFIG['sponsors'].present?
  .sponsors-header Open Build Service is sponsored by
  .container-fluid#sponsor
    .container-xxl.sponsors.d-flex.flex-wrap.justify-content-center
      - CONFIG['sponsors'].each do |sponsor|
        .sponsor-item
          = link_to_sponsor(sponsor)

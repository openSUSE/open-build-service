- if CONFIG['sponsors'].present?
  - sponsor = CONFIG['sponsors'].sample
  .card.mb-3
    %h5.card-header Sponsor
    .card-body.text-center
      %figure.figure.mb-0
        = link_to_sponsor(sponsor)
        %figcaption.figure-caption.mt-1
          = sponsor['description']

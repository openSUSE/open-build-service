%b
  Submit update from
  = project_or_package_link project: action[:sprj], package: action[:spkg], homeproject: creator
  to
  = project_or_package_link project: action[:tprj], package: action[:tpkg]
- if action[:releaseproject]
  %br/
  %i
    Release in
    = action[:releaseproject]

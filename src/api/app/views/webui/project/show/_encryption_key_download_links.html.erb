<% if @project.key_info.present? %>
  <li>
    <%= link_to(sprited_text('key', 'GPG Key / SSL Certificate'), key_dialog_project_public_key_path(project_name: @project.name), remote: true, id: 'gpg-project') %>
  </li>
<% end %>

<% content_for(:content_for_head, javascript_include_tag('webui/application/cm2/index-xml')) %>

<% @pagetitle = "Meta Configuration of Package #{@package}" %>
<% package_bread_crumb('Meta') %>

<%= render :partial => 'tabs' %>

<h3><%= @pagetitle %></h3>
<% if User.current.can_modify? @package %>
  <%= render partial: "shared/editor", locals: {text: @meta, mode: 'xml', save: {url: package_save_meta_path, data: {project: @project.name, package: @package.name, submit: 'meta'}}} %>
<% else %>
  <%= render partial: "shared/editor", locals: {text: @meta, mode: 'xml', style: {read_only: true}} %>
<% end %>

= render ModalComponent.new(modal_id: "distro-modal-#{model.id}",
                            modal_button_data: {text: text, type: 'link text-decoration-none', icon: icon}) do |component|
  - component.with_header do
    = text
  = render partial: 'webui/distros/form', locals: {url:, model:}

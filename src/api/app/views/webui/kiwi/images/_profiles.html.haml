.nested-fields
  %h2
    Profiles
  - if @image.profiles.empty?
    %p There are no profiles
  - else
    %ul
      - @image.profiles.each do |profile|
        %li
          = f.fields_for(:profiles, profile) do |profile_fields|
            = profile_fields.check_box(:selected)
            = profile_fields.label(:selected, profile.name)

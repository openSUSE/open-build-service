- @pagetitle = "New vendor"
- content_for :title, "New vendor"

.card
  = render(partial: 'webui/project/tabs', locals: { project: @project })
  .card-body
    %h1 New vendor

    = render(partial: 'webui/vendors/form', locals: { vendor: @vendor, url: project_vendor_path(@project) })

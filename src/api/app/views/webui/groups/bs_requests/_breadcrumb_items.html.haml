- case action_name
- when 'index'
  %li.breadcrumb-item
    - if User.admin_session?
      = link_to 'Manage Groups', groups_path
    - else
      Groups
  %li.breadcrumb-item.active{ 'aria-current' => 'page' }
    = @user_or_group

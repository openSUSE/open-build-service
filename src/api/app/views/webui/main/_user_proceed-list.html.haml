- if User.session
  = link_to(user_path(User.session!), class: 'nav-link', title: 'Go to your profile') do
    %i.fas.fa-user.fa-2x
    %br
    Profile
= link_to(projects_path, class: 'nav-link', title: 'See all projects') do
  %i.fas.fa-list.fa-2x.text-muted
  %br
  All Projects
- if User.session
  = link_to(new_project_path, ns: User.session!.home_project_name, class: 'nav-link', title: 'Create a new project') do
    %i.fas.fa-plus-square.fa-2x.text-primary
    %br
    New Project
= link_to(image_templates_path, class: 'nav-link', title: 'Create a new image') do
  %i.fas.fa-compact-disc.fa-2x.text-secondary
  %br
  New Image


xml.instruct!

xml.collection do
  @list.each do |item|

    # item should always be a package, only packages can be downloaded
    if item.instance_of? DbPackage
      xml.package(
        :name => item.name,
        :project => item.db_project.name,
        :downloads => item.downloads
      )

    end
  end
end


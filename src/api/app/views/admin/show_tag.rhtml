<% for column in Tag.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @tag.send(column.name) %>
</p>
<% end %>
<b> belongs to: </b><br/><br/>
<%= @tagged_items.map {|item| item.name}.join(", ") %>

<br/><br/>
<%= link_to 'Edit', :action => 'edit_tag', :id => @tag %> |
<%= link_to 'Back', :action => 'list_tags' %>

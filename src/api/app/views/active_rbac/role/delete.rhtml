<h2>Deleting Role &quot;<%= @role.title %>&quot;</h2>

<p>
Are you sure you want to delete the role with the name &quot;<%= @role.title %>&quot;?
All users will be unassigned from this role and all permissions will be revoked from
it. <strong>This action cannot be reverted</strong>.
</p>

<% form_for :role, :url => { :action => :delete, :id => @role } do |form| %>
  <%= submit_tag 'Yes', :name => 'yes' %>
  <%= submit_tag 'No', :name => 'no' %>
<% end %>
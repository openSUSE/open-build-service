<% notifications.each do |notification| %>
  <%= render(
        template: "event_mailer/#{notification.template_name}",
        locals: { event: notification.event_expanded_payload },
        layout: false
      )
    %>
  <p>---</p>
<% end %>

%ul.list-inline.d-flex.flex-row-reverse.avatars.m-0
  - if number_of_hidden_users.positive?
    %li.list-inline-item
      %span.rounded-circle.bg-body-secondary.border.avatars-counter{ title: "#{number_of_hidden_users} more users involved" }
        +#{number_of_hidden_users}
  - avatars_to_display.each do |avatar_object|
    %li.list-inline-item
      - case avatar_object.class.name
      - when 'User', 'Group'
        = render(AvatarComponent.new(name: avatar_object.name, email: avatar_object.email, size: 23, shape: :circle))
      - when 'Package'
        %span.fa.fa-archive.text-warning.rounded-circle.bg-body-secondary.border.simulated-avatar{ title: package_title(avatar_object) }
      - when 'Project'
        %span.fa.fa-cubes.text-secondary.rounded-circle.bg-body-secondary.border.simulated-avatar{ title: project_title(avatar_object) }

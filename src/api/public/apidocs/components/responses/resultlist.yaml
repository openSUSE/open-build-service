description: |
  OK.

  XML Schema used for body validation: [buildresult.rng](../schema/buildresult.rng)
content:
  application/xml; charset=utf-8:
    schema:
      $ref: '../schemas/resultlist.yaml'

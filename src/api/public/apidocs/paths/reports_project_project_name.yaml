get:
  summary: List all the project's reports
  description: List all the reports for the project.
  security:
    - basic_authentication: []
  parameters:
    - $ref: '../components/parameters/project_name.yaml'
  responses:
    '200':
      description: OK. The request has succeeded.
      content:
        application/xml; charset=utf-8:
          schema:
            $ref: '../components/schemas/reports.yaml'
          example: |
            <reports>
              <report project="linked_project" id="12" category="spam" created_at="2026-09-18 10:07:00 UTC" updated_at="2026-09-18 10:07:00 UTC">
              </report>
            </reports>
    '401':
      $ref: '../components/responses/unauthorized.yaml'
    '404':
      description: |
        Not found.

        XML Schema used for body validation: [status.rng](../schema/status.rng)
      content:
        application/xml; charset=utf-8:
          schema:
            $ref: '../components/schemas/api_response.yaml'
          examples:
            not_found:
              summary: 'Project not found: notaproject'
              value:
                code: not_found
                summary: 'Project not found: notaproject'
  tags:
    - Reports

post:
  summary: Create a project report
  description: Create a report for the project.
  security:
    - basic_authentication: []
  parameters:
    - $ref: '../components/parameters/project_name.yaml'
    - in: query
      name: category
      schema:
        type: string
        enum: [spam, scam, forbidden_license, illegal_content, other]
      description: Category of the report
      example: spam
  requestBody:
    description: Content of the report
    required: true
    content:
      text/plain:
        schema:
          type: string
        example: This is a very valuable report
  responses:
    '200':
      $ref: '../components/responses/succeeded.yaml'
    '400':
      description: |
        Invalid report.

        XML Schema used for body validation: [status.rng](../schema/status.rng)
      content:
        application/xml; charset=utf-8:
          schema:
            $ref: '../components/schemas/api_response.yaml'
    '401':
      $ref: '../components/responses/unauthorized.yaml'
    '404':
      description: |
        Not found.

        XML Schema used for body validation: [status.rng](../schema/status.rng)
      content:
        application/xml; charset=utf-8:
          schema:
            $ref: '../components/schemas/api_response.yaml'
          examples:
            not_found:
              value:
                code: not_found
  tags:
    - Reports

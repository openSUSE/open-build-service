# the web ui only uses the database to store jobs to do (render plots)

production:
  adapter: mysql
  database: webui_production
  username: root
  password:

development:
  adapter: sqlite3
  database: db/database.db

stage:
  adapter: sqlite3
  database: db/database.db

test:
  adapter: sqlite3
  database: db/database.db

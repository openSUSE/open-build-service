<% @pagetitle = "List My Projects" %>
<%
 @crumb_list = ['My Projects']
-%>

<h1>Watched Projects</h1>
<%= render :partial => 'watch_list' %>

<h1>Involved Projects</h1>
 <% if @iprojects and not @iprojects.empty? -%>
  <table id='involved_prj-table' class='grid' title="Involved Projects">
   <tr>
    <th>Project</th>
    <th>Monitor</th>
   </tr>
 <% @iprojects.each do |project| -%>
   <tr>
    <td><%= link_to project, :action => :show, :project => project %></td>
    <td><%= link_to image_tag('monitor.png', :size => '16x16', :alt => 'Monitor', :title => 'Monitor'), :action => :monitor, :project => project %></td>
   </tr>
  <% end -%>
  </table>
  <% else %>
  <p><em>Not involved in any project.</em></p>
  <% end -%>

  <h1>Involved Packages</h1>
    <table id='involved_pkg-table' class='grid' title="Involved Packages">
      <tr>
        <th>Package</th>
        <th>Project</th>
        <th>Monitor</th>
      </tr>
      <% @ipackages.each do |prj, packs| -%>
        <% packs.each do |pkg| -%>
          <tr>
            <td><%= link_to pkg, :controller => :package, :action => :show, :project => prj, :package => pkg %></td>
            <td><%= link_to prj, :action => :show, :project => prj %></td>
            <td><%= link_to image_tag('monitor.png', :size => '16x16', :alt => 'Monitor', :title => 'Monitor'), :action => :monitor, :project => prj, :pkgname => pkg %></td>
          </tr>
        <% end -%>
      <% end -%>
   </table>


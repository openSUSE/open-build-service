<% if @configuration['download_url'] %>
  <% url = "#{@configuration['download_url']}/#{project.to_s.gsub(/:/,':/')}/#{repository}" %>
  <%= link_to(image_tag('drive_web.png'), url) %>
  <%= link_to('Go to download repository', url) %>
<% end %>

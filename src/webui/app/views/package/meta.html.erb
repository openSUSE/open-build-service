<% @pagetitle = "Raw Configuration of #{@package}"
   package_bread_crumb('Raw Config')
-%>

<%= render :partial => "tabs" %>

<h3><%= @pagetitle %></h3>

<% if @user %>
  <p>
    <%= link_to(image_tag('icons/package_edit.png', :title => 'Edit raw configuration' ), :action => :edit_meta, :project => @project, :package => @package) %>
    <%= link_to('Edit raw configuration', :action => :edit_meta, :project => @project, :package => @package) %>
  </p>
<% end %>

<%= render :partial => "shared/syntaxhighlighter", :locals => {:code => @meta, :type => 'xml'} %>

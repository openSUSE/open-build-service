<% @pagetitle = "Requests for #{@package}" %>
<% package_bread_crumb( "Requests" ) -%>

<%= render :partial => "tabs" %>

<h2>Pending requests of package <%=h @package.name %> (#<%= @current_requests.size %>)</h2>

<%= render :partial => 'shared/list_requests',
  :locals => { :requests => @current_requests, :hide_package => false, :hide_project => true } %>


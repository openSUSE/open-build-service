<% @pagetitle = "Requests for #{h(@package)}" %>
<% package_bread_crumb( "Requests" ) -%>

<%= render :partial => "tabs" %>

<h3><%= @pagetitle %></h3>

<%= render :partial => 'shared/select_requests', :locals => { :source => :package, :project => @project, :package => @package} %>

